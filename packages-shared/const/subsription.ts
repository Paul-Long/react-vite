export enum KlineSubTypes {
  Subscribe = 'kline-subscribe',
  Unsubscribe = 'kline-unsubscribe',
}

export enum TickSubTypes {
  Subscribe = 'tick-subscribe',
  Unsubscribe = 'tick-unsubscribe',
}

export enum MarkPriceSubTypes {
  Subscribe = 'mark-price-subscribe',
  Unsubscribe = 'mark-price-unsubscribe',
}

export enum RatePriceSubTypes {
  Subscribe = 'rate-price-subscribe',
  Unsubscribe = 'rate-price-unsubscribe',
}

export enum LastTradeSnapshotSubTypes {
  Subscribe = 'last-trade-snapshot-subscribe',
  Unsubscribe = 'last-trade-snapshot-unsubscribe',
}

export enum ReferencePriceSubTypes {
  Subscribe = 'reference-price-subscribe',
  Unsubscribe = 'reference-price-unsubscribe',
}

export enum PositionSubTypes {
  Subscribe = 'position-subscribe',
  Unsubscribe = 'position-unsubscribe',
}
